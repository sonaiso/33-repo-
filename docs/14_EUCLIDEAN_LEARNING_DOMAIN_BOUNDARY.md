# Euclidean Learning Domain Boundary Map

## Authority
- `docs/00_MAQOOL_CONSTITUTION.md §5`
- `docs/12_RUNTIME_EMBARGO_CONSTITUTION.md`
- `docs/15_PROJECT_ROADMAP.md §Euclidean Learning Track`

## Scope
This document defines a non-executive mapping between Euclidean learning audit labels and canonical constitutional domains.

The mapping is boundary guidance only. It does not authorize runtime domain opening.

## Audit-Only Layer → Domain Boundary Map
- `T8_PHONIC_SIGNIFIER` → `D1_DAL_ONLY` (audit-only)
- `T9_RAW_MEANING` → bridge-required (`None` in mapping, cannot emit meaning)
- `T10_CONVENTIONAL` → `D3_LEXICAL_MADLUL` (audit-only, not opened)
- `T11_RELATION_AUDIT` → `D4_RELATION` (audit-only, not opened)
- `T12_IFADAH_AUDIT` → `D5_IFADAH` (audit-only, not opened)
- `T13_HUKM_AUDIT` → `D6_HUKM` (audit-only, not opened)

## Binding Boundary Law
- LearningLayer label is not Domain opening.
- Audit relation is not Relation runtime.
- Audit ifadah is not Ifadah runtime.
- Audit hukm is not Hukm runtime.

## Runtime Embargo Alignment
- Euclidean learning remains `AUDIT_SANDBOX_ONLY`.
- Euclidean learning decisions remain `EUCLIDEAN_LEARNING_AUDIT_ONLY`.
- Euclidean outputs are non-authoritative and remain rank `CANDIDATE` for constitutional entities.
- This boundary map cannot be used to open `D3/D4/D5/D6` runtime domains.
